package com.android.module2.realstatebroker;

public class Broker {


    // Contar con un sistema de registro de sus trabajadores. 
    // C/U necesita: - Nombres 
    //  - Apellidos
    // - RUN (se debe almacenar con guion)
    //   - Teléfono
    // - Edad 
    // crear clase que represente a un trabajador de la empresa,
    // usando atributos con tipos de datos adecuados a la situación. 
    //- Debe agregar un método toString(). 
    //- Debe tener dos métodos constructores: 
    //1-uno que cree un objeto vacío, y 
    //2-otro que solicite todos los atributos pertenecientes a la clase.
    // - Debe agregar el método nombreCompleto()==>retornar la concatenación==> nombres y apellidos del usuario.
    // - Desarrolle el método descomponerRun(), ==> retornar un número entero correspondiente a todo lo que está en el RUN antes del guion y del dígito verificador. 
    //. Nota 2: Recuerde que esta clase, a diferencia de las anteriores desarrolladas, no debe llevar un método main(), ya que desde ella no se ejecutarán acciones.
    
}
